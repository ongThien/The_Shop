<article class="product-item">
  <img src="<%= product.imageUrl %>" alt="<%= product.title %>">
  <h2><%= product.title %></h2>
  <div>
    <a class="btn btn-alt" href="/amin/products/<%= product.id %>">View & Edit</a>
    <button class="btn btn-alt">Delete</button>
  </div>
</article>